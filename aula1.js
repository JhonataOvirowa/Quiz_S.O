const subtrair = function(x, y) {
    let resultado = x - y
    console.log(`${x} - ${y} = ${resultado}`)
    return resultado
}

const r = subtrair(10, 10)

console.log(`o resultado foi ${r}`)

console.log(subtrair)